# --- Core data & ML ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# --- Deep learning / transformers ---
torch>=2.1.0
transformers>=4.36.0
datasets>=2.15.0

# --- RAG / embeddings / vector search ---
sentence-transformers>=2.2.2
faiss-cpu>=1.7.4

# --- LLM API client ---
# Google AI Studio (Gemini) — free-tier API key: https://aistudio.google.com/apikey
# NOTE: this is the current `google-genai` package, not the deprecated
# `google-generativeai` package (which Google has stopped updating).
google-genai>=1.0.0
# Optional, only needed if you use the LangChain reference agent in agent.py
langchain-google-genai>=2.0.0

# --- Agent / orchestration ---
# (kept dependency-free by default; see src/agent.py for a LangChain variant)

# --- Deployment ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# --- Dashboard / visualization ---
streamlit>=1.31.0
plotly>=5.18.0
matplotlib>=3.8.0
seaborn>=0.13.0
